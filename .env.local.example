# Copy to .env.local and fill in values from Supabase Dashboard.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Required to apply SQL migrations locally (Project Settings → Database → Connection string).
# Use the Session pooler URI (port 6543) or direct connection (port 5432).
DATABASE_URL=
